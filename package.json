{
  "type": "commonjs",
  "name": "node-counter-redis",
  "dependencies": {
    "express": "^4.21.0",
    "pug": "^3.0.3",
    "redis": "^4.7.0",
    "dotenv": "^16.4.5"
  },

  "devDependencies": {
    "pm2": "^5.4.2"
  },

  "scripts": {
    "start": "node index.js",
    "dev": "npx pm2-dev index.js",
    "start-p": "npx pm2 start index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
